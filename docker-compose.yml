version: '3.8'

services:
  web:
    build: .
    container_name: purely-planned-website
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - web-network

networks:
  web-network:
    driver: bridge
